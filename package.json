{
  "name": "verona-interfaces_specification_editor",
  "license": "CC0-1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/verona-interfaces/editor"
  },
  "scripts": {
    "generate": "asyncapi generate fromTemplate ./editorapi.yaml @asyncapi/html-template -o ./docs --force-write -p singleFile=true"
  },
  "devDependencies": {
    "@asyncapi/cli": "^0.51.4",
    "@asyncapi/html-template": "^0.28.3"
  }
}
