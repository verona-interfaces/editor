{
  "name": "verona-interfaces_specification_editor",
  "license": "CC0-1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/verona-interfaces/editor"
  },
  "scripts": {
    "generate": "asyncapi generate fromTemplate ./editorapi.yaml @asyncapi/html-template -o ./docs --force-write --use-new-generator -p singleFile=true"
  },
  "devDependencies": {
    "@asyncapi/cli": "^2.11.0",
    "@asyncapi/html-template": "^3.1.0"
  }
}
